[project]
name = "api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "google-genai",
    "python-dotenv",
    "python-multipart>=0.0.32",
]

[dependency-groups]
dev = [
    "pytest",
]
